diff --git a/apps/version.c b/apps/version.c
index cab17a46bf..d4ae26e829 100644
--- a/apps/version.c
+++ b/apps/version.c
@@ -45,6 +45,7 @@ int version_main(int argc, char **argv)
     int ret = 1, dirty = 0, seed = 0;
     int cflags = 0, version = 0, date = 0, options = 0, platform = 0, dir = 0;
     int engdir = 0, moddir = 0, cpuinfo = 0;
+    int engines = 0;
     char *prog;
     OPTION_CHOICE o;
 
@@ -93,7 +94,7 @@ opthelp:
         case OPT_A:
             seed = options = cflags = version = date = platform
                 = dir = engdir = moddir = cpuinfo
-                = 1;
+                = engines = 1;
             break;
         }
     }
@@ -132,6 +133,16 @@ opthelp:
     }
     if (cpuinfo)
         printf("%s\n", OpenSSL_version(OPENSSL_CPU_INFO));
+    if (engines) {
+        ENGINE *e;
+        printf("engines:  ");
+        e = ENGINE_get_first();
+        while (e) {
+            printf("%s ", ENGINE_get_id(e));
+            e = ENGINE_get_next(e);
+        }
+        printf("\n");
+    }
     ret = 0;
  end:
     return ret;
